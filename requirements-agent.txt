# Agent-only dependencies (NOT for backend/Railway)
# These are installed locally to build KazumeeAgent.exe with PyInstaller
# Do NOT install on Railway backend — evdev (Linux dependency of pynput) will fail to compile

# Global hotkey support (Windows only)
# Used in kazumee-agent.py:setup_hotkey_listener() for Ctrl+Shift+C capture
pynput==1.8.2

# Everything else comes from the main requirements.txt
# To build the agent locally:
#   pip install -r requirements.txt -r requirements-agent.txt
#   pyinstaller KazumeeAgent.spec
